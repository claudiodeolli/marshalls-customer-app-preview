# ── Chunks de contexto gerados para o Claude ──
*-chunks/
all-chunks/

# ── Configurações locais do Claude Code ──
.claude/settings.local.json

# ── Dependências ──
node_modules/

# ── Build / output ──
.next/
out/
dist/

# ── Variáveis de ambiente ──
.env
.env.local
.env.*.local

# ── Logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editores ──
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# ── TypeScript ──
*.tsbuildinfo

.vercel
.env*

# ── whatsapp-triage (comando /whatsapp-issues) ──
whatsapp-triage/.auth/
whatsapp-triage/.state/
whatsapp-triage/tmp/
whatsapp-triage/qr.png
# Pacotes de sessão gerados por sessionBackup.mjs — são credenciais da conta
# do WhatsApp, nunca podem entrar no repositório.
*session*.json
!whatsapp-triage/.env.example
