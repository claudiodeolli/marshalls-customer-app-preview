# Vercel: vazio → chamadas /api/* são relativas e passam pelo proxy do next.config.mjs
NEXT_PUBLIC_API_BASE_URL=
# Domínio usado no /api/webapp para buscar clientId e tema — sempre o de produção
NEXT_PUBLIC_WEBAPP_DOMAIN=prontoatendimento.marshallsmed.com.br
